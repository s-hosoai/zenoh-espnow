diff --git a/include/zenoh-pico/config.h b/include/zenoh-pico/config.h
index 41829f7..69cf872 100644
--- a/include/zenoh-pico/config.h
+++ b/include/zenoh-pico/config.h
@@ -22,7 +22,9 @@
 /*--- CMake generated config; pass values to CMake to change the following tokens ---*/
 #define Z_FRAG_MAX_SIZE 4096
 #define Z_BATCH_UNICAST_SIZE 2048
+#ifndef Z_BATCH_MULTICAST_SIZE
 #define Z_BATCH_MULTICAST_SIZE 2048
+#endif
 #define Z_CONFIG_SOCKET_TIMEOUT 100
 #define Z_TRANSPORT_LEASE 10000
 #define Z_TRANSPORT_LEASE_EXPIRE_FACTOR 3
diff --git a/src/system/espidf/network.c b/src/system/espidf/network.c
index 26e955b..fb61a9e 100644
--- a/src/system/espidf/network.c
+++ b/src/system/espidf/network.c
@@ -396,6 +396,7 @@ size_t _z_send_udp_unicast(const _z_sys_net_socket_t sock, const uint8_t *ptr, s
 #endif
 
 #if Z_FEATURE_LINK_UDP_MULTICAST == 1
+#if !defined(ZENOH_ESPNOW_LINK_OVERRIDE)
 z_result_t _z_open_udp_multicast(_z_sys_net_socket_t *sock, const _z_sys_net_endpoint_t rep, _z_sys_net_endpoint_t *lep,
                                  uint32_t tout, const char *iface) {
     z_result_t ret = _Z_RES_OK;
@@ -733,7 +734,8 @@ size_t _z_send_udp_multicast(const _z_sys_net_socket_t sock, const uint8_t *ptr,
                              const _z_sys_net_endpoint_t rep) {
     return sendto(sock._fd, ptr, len, 0, rep._iptcp->ai_addr, rep._iptcp->ai_addrlen);
 }
-#endif
+#endif  /* !defined(ZENOH_ESPNOW_LINK_OVERRIDE) */
+#endif  /* Z_FEATURE_LINK_UDP_MULTICAST */
 
 #if Z_FEATURE_LINK_SERIAL == 1
 /*------------------ Serial sockets ------------------*/
diff --git a/src/system/espidf/system.c b/src/system/espidf/system.c
index b19873c..aad83fc 100644
--- a/src/system/espidf/system.c
+++ b/src/system/espidf/system.c
@@ -170,10 +170,10 @@ z_result_t _z_mutex_rec_unlock(_z_mutex_rec_t *m) { _Z_CHECK_SYS_ERR(pthread_mut
 
 /*------------------ Condvar ------------------*/
 z_result_t _z_condvar_init(_z_condvar_t *cv) {
-    pthread_condattr_t attr;
-    pthread_condattr_init(&attr);
-    pthread_condattr_setclock(&attr, CLOCK_MONOTONIC);
-    _Z_CHECK_SYS_ERR(pthread_cond_init(cv, &attr));
+    // pthread_condattr_setclock(CLOCK_MONOTONIC) is not supported on ESP-IDF;
+    // the call is silently ignored and produces a logspam warning.
+    // _z_condvar_wait_until handles the MONOTONIC→REALTIME conversion instead.
+    _Z_CHECK_SYS_ERR(pthread_cond_init(cv, NULL));
 }
 
 z_result_t _z_condvar_drop(_z_condvar_t *cv) { _Z_CHECK_SYS_ERR(pthread_cond_destroy(cv)); }
@@ -185,12 +185,37 @@ z_result_t _z_condvar_signal_all(_z_condvar_t *cv) { _Z_CHECK_SYS_ERR(pthread_co
 z_result_t _z_condvar_wait(_z_condvar_t *cv, _z_mutex_t *m) { _Z_CHECK_SYS_ERR(pthread_cond_wait(cv, m)); }
 
 z_result_t _z_condvar_wait_until(_z_condvar_t *cv, _z_mutex_t *m, const z_clock_t *abstime) {
-    int error = pthread_cond_timedwait(cv, m, abstime);
+    // ESP-IDF silently ignores pthread_condattr_setclock(CLOCK_MONOTONIC), so
+    // pthread_cond_timedwait uses REALTIME.  abstime is MONOTONIC-based, which
+    // would make the wait expire immediately.  Fix: convert the remaining
+    // MONOTONIC duration to a REALTIME-based absolute deadline.
+    struct timespec mono_now;
+    clock_gettime(CLOCK_MONOTONIC, &mono_now);
+
+    int64_t remaining_ns =
+        ((int64_t)abstime->tv_sec  - mono_now.tv_sec)  * 1000000000LL +
+        ((int64_t)abstime->tv_nsec - mono_now.tv_nsec);
+
+    if (remaining_ns <= 0) {
+        return Z_ETIMEDOUT;
+    }
+
+    struct timespec real_now;
+    clock_gettime(CLOCK_REALTIME, &real_now);
 
+    struct timespec deadline = {
+        .tv_sec  = real_now.tv_sec  + (time_t)(remaining_ns / 1000000000LL),
+        .tv_nsec = real_now.tv_nsec + (long)(remaining_ns % 1000000000LL),
+    };
+    if (deadline.tv_nsec >= 1000000000L) {
+        deadline.tv_sec++;
+        deadline.tv_nsec -= 1000000000L;
+    }
+
+    int error = pthread_cond_timedwait(cv, m, &deadline);
     if (error == ETIMEDOUT) {
         return Z_ETIMEDOUT;
     }
-
     _Z_CHECK_SYS_ERR(error);
 }
 #endif  // Z_FEATURE_MULTI_THREAD == 1
